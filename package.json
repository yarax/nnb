{
  "name": "nnb",
  "version": "0.0.6",
  "main": "index.js",
  "scripts": {
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "rebuild" : "node-gyp configure && node-gyp build",
    "prebuild" : "npm install nan",
    "test": "mocha"
  },
  "devDependencies": {
    "nan": "^2.1.0",
    "mocha": "^2.2.5",
    "node-gyp": "^3.2.0"
  },
  "dependencies": {
    "bindings": "^1.2.1"
  }
}
